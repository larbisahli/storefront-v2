{
  "buildCommand": "pnpm --workspace-root exec turbo --no-daemon --filter template1 build --env-mode loose",
  "installCommand": "node -v && pnpm --workspace-root --filter template1... install --config.dedupe-peer-dependents=false",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}