{
  "buildCommand": "pnpm --workspace-root exec turbo --no-daemon --filter template3 build --env-mode loose",
  "installCommand": "node -v && pnpm --workspace-root --filter template3... install --config.dedupe-peer-dependents=false",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}